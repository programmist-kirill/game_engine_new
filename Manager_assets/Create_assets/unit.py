import compilation_asset

asset = "   dasdasdas"
index = compilation_asset.draw_asset.main(asset , index=0)

compilation_asset.draw_asset.main()



count_x = compilation_asset.compilation_asset.launch_the_required_function(first_value="10")
count_y = compilation_asset.compilation_asset.determing_y_coordinates(first_value=31)

print(f"count_x = {count_x}")
print(f"count_y = {count_y}")

print(f"\n\n{type(count_x)}")
